
mkdir -m 0755 -p "$generatedMachineRootOverlaysPath"/etc/sysconfig

# Could we source this from somewhere?
local domain="slc.co.uk"

cat >"$generatedMachineRootOverlaysPath"/etc/sysconfig/network <<-EOF
	NETWORKING=yes
	NETWORKING_IPV6=no
	HOSTNAME=${machine}.${domain}
EOF
